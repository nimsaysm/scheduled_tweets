<div class="d-flex flex-column align-items-center justify-content-center">
    <h1>Welcome to Scheduled Tweets</h1>
</div>

<% if Current.user.nil? %>
<% else %>
    <%= form_tag('/auth/twitter2', method: :post, data: {turbo: "false"}) do %>
        <button type="submit" class="btn btn-primary">Connect Twitter</button>
    <% end %>
<% end %>